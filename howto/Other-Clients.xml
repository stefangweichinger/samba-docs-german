<?xml version="1.0" encoding="ISO-8859-1"?>
<chapter id="Other-Clients">
<chapterinfo>
	&author.jelmer;
	&author.jht;
	&author.danshearer;
	<author>&person.jmcd;<contrib>OS/2</contrib></author>
	<author>&person.sgw;<contrib>Deutsche Übersetzung</contrib></author>
</chapterinfo>

<title>Samba und andere CIFS-Clients</title>

<para>Dieses Kapitel enthält clientspezifische Informationen.</para>

<sect1>
<title>Macintosh-Clients</title>

<para>
Ja, <ulink url="http://www.thursby.com/">Thursby</ulink> hat einen CIFS-Client/Server namens <ulink url="http://www.thursby.com/products/dave.html">DAVE.</ulink>
Er wird gegen Windows 95, Windows NT /200x/XP und Samba auf Kompatibilität geprüft. Bei Verfassen
dieses Textes war DAVE in Version 4.1 erhältlich. Bitte konsultieren Sie Thursbys Webseite für mehr
Informationen zu diesem Produkt.
</para>

<para> 
Alternativen &smbmdash; Es gibt zwei freie Implementationen von Appletalk für einige Arten von
UNIX-Maschinen und noch einige kommerzielle mehr. Diese Produkte erlauben es Ihnen, 
native Datei- und Druck-Dienste für Macintosh-Benutzer anzubieten, ohne dass weitere Unterstützung
dafür auf dem Macintosh erforderlich wäre.
Die zwei freien Implementationen sind
<ulink url="http://www.umich.edu/~rsug/netatalk/">Netatalk</ulink> und
<ulink url="http://www.cs.mu.oz.au/appletalk/atalk.html">CAP.</ulink>
Diese Packages bieten Macs das, was Samba Benutzern von MS Windows bietet.
Für mehr Informationen zu diesen Packages, Samba und Linux (sowie anderen UNIX-basierenden Systemen)
sehen Sie sich bitte 
<ulink noescape="1" url="http://www.eats.com/linux_mac_win.html">http://www.eats.com/linux_mac_win.html</ulink> an.
</para>

<para>Neuere Versionen des Macintosh (Mac OS X) enthalten Samba.</para>

</sect1>

<sect1>
<title>OS/2-Clients</title>

	<sect2>
		<title>Das Konfigurieren von OS/2 Warp Connect oder OS/2 Warp 4</title>

		<para>Grundlegend brauchen Sie drei Komponenten:</para>
		
		<itemizedlist>
			<listitem>Den Datei- und Druck-Client (IBM Peer)</listitem>
			<listitem>TCP/IP (Internet-Support) </listitem>
			<listitem>Den <quote>NetBIOS über TCP/IP</quote>-Treiber (TCPBEUI)</listitem>
		</itemizedlist>
		
		<para>Wie Sie die ersten beiden gemeinsam mit dem Basis-Betriebssystem
		auf einem leeren System installieren, ist im Warp-Handbuch beschrieben. Wenn Warp bereits
		installiert wurde, Sie aber nun die Netzwerk-Unterstützung installieren wollen,
		verwenden Sie den Eintrag <quote>Selective Install for Networking</quote>
		im Ordner <quote>System Setup</quote>.</para>

		<para>Das Hinzufügen des <quote>NetBIOS über TCP/IP</quote>-Treibers wird nicht
		im Handbuch beschrieben und auch von der Online-Dokumentation nur gestreift.
		Starten Sie <command>MPTS.EXE</command>, klicken Sie auf <guiicon>OK</guiicon>,
		dann auf <guimenu>Configure LAPS</guimenu> und auf 
		<guimenu>IBM OS/2 NETBIOS OVER TCP/IP</guimenu> unter <guilabel>Protocols</guilabel>.
		Diese Zeile wird dann in die <guilabel>Current Configuration</guilabel> verschoben.
		Wählen Sie diese Zeile aus, klicken Sie auf <guimenuitem>Change number</guimenuitem>,
		und erhöhen Sie den Wert von 0 auf 1. Speichern Sie diese Konfiguration.</para>

		<para>Wenn der Samba-Server nicht in Ihrem lokalen Subnetz ist, können Sie
		optional IP-Namen und -Adressen dieser Server zur <guimenu>Names List</guimenu>
		hinzufügen oder einen WINS-Server (NetBIOS Nameserver, in der 
		Terminologie von IBM und RFC) spezifizieren. Für Warp Connect brauchen Sie
		eventuell ein Update für den <constant>IBM Peer</constant>, um ihn auf dasselbe
		Level wie Warp 4 zu bringen. Sehen Sie dazu auf der oben genannten Webseite nach.</para>

	</sect2>
	
	<sect2>
		<title>Andere Versionen von OS/2 konfigurieren</title>
	
		<para>Dieser Abschnitt behandelt die Konfiguration von OS/2 Warp 3 (nicht Connect), OS/2 1.2, 1.3 oder 2.x.</para>
		
		<para>Sie können den freien Microsoft LAN Manager 2.2c Client für OS/2 
		verwenden, der unter
		<ulink noescape="1" url="ftp://ftp.microsoft.com/BusSys/Clients/LANMAN.OS2/">
		ftp://ftp.microsoft.com/BusSys/Clients/LANMAN.OS2/</ulink> erhältlich ist.
		Kurz gesagt: Editieren Sie die Datei 
		<filename>\OS2VER</filename> im root-Verzeichnis der OS/2-Boot-Partition, 
		und fügen Sie die Zeilen</para>
	
		<para><programlisting>
		20=setup.exe
		20=netwksta.sys
		20=netvdd.sys
		</programlisting></para>
		
		<para>ein, bevor Sie den Client installieren. 
		Verwenden Sie den NICHT den vorhandenen NE2000-Treiber, da er fehlerhaft ist.
		Probieren Sie stattdessen den NE2000- oder NS2000-Treiber von
		<ulink noescape="1" url="ftp://ftp.cdrom.com/pub/os2/network/ndis/">
 		ftp://ftp.cdrom.com/pub/os2/network/ndis/</ulink> aus.
		</para>
	</sect2>
	
	<sect2>
		<title>Druckertreiber-Download für OS/2-Clients</title>

		<para>Legen Sie eine Freigabe namens <smbconfsection>[PRINTDRV]</smbconfsection> an,
		die world-readable ist. Kopieren Sie Ihre OS/2-Treiber-Dateien dorthin.
		Die <filename>.EA_</filename>-Dateien müssen jedoch weiter separat beleiben, also
		werden Sie die originalen Installationsdateien benutzen müssen und dürfen keinen
		installierten Treiber von einem OS/2-System kopieren.
		</para>
		
		<para>Installieren Sie zuerst den NT-Treiber für diesen Drucker. Dann fügen Sie den 
		Parameter 
		<smbconfoption><name>os2 driver map</name><value><replaceable>filename</replaceable></value></smbconfoption> 
		zu Ihrer Datei &smb.conf; hinzu.

		Als Nächstes weisen Sie in der Datei, die durch <replaceable>filename</replaceable>
		bezeichnet wird, den NT-Treibernamen dem OS/2-Treibernamen wie folgt zu:</para>

		<para><parameter><replaceable>nt driver name</replaceable> = <replaceable>os2 driver name</replaceable>.<replaceable>device name</replaceable></parameter>, e.g.</para>

		<para><parameter>
		HP LaserJet 5L = LASERJET.HP LaserJet 5L</parameter></para>

		<para>Sie können in dieser Datei mehrere Treiberzuweisungen vornehmen.</para>
	
		<para>Wenn Sie nur den OS/2-Treibernamen angeben, aber nicht den Device-Namen,
		werden im ersten Versuch zwar die Dateien geladen, aber der OS/2-Client wird melden,
		dass der Treiber nicht verfügbar ist. Im zweiten Anlauf wird es funktionieren.
		Dies kann durch einfaches Hinzufügen des Device-Namens behoben werden, wonach
		es schon beim ersten Versuch funktioniert.
		</para>
	</sect2>
</sect1>

<sect1>
<title>Windows for Workgroups</title>

<sect2>
<title>Neuester TCP/IP-Stack von Microsoft</title>

<para>Verwenden Sie den neuesten TCP/IP-Stack von Microsoft, wenn Sie Windows
for Workgroups einsetzen. Die frühen TCP/IP-Stacks hatten viele Bugs.</para>

<para> 
Microsoft hat ein inkrementelles Upgrade für seine TCP/IP-32-Bit-VxD-Treiber veröffentlicht. Die letzte Release kann auf der FTP-Site
ftp.microsoft.com
in <filename>/peropsys/windows/public/tcpip/wfwt32.exe</filename> gefunden werden.
Es gibt dort auch eine Datei update.txt, die die gelösten Probleme beschreibt.
Neue Dateien sind:
<filename>WINSOCK.DLL</filename>, 
<filename>TELNET.EXE</filename>, 
<filename>WSOCK.386</filename>, 
<filename>VNBT.386</filename>,
<filename>WSTCP.386</filename>, 
<filename>TRACERT.EXE</filename>, 
<filename>NETSTAT.EXE</filename> und 
<filename>NBTSTAT.EXE</filename>.
</para>

</sect2>

<sect2>
<title>Das Löschen von .pwl-Dateien nach Passwort-Änderungen</title>

<para>
Windows for Workgroups arbeitet lausig, wenn es um Passwörter geht.
Wenn Sie Passwörter auf entweder der UNIX-Mascine oder dem PC ändern, ist es am sichersten, die
.pwl-Dateien im Windows-Verzeichnis zu löschen. Der PC wird sich darüber beschweren, diese Dateien
nicht zu finden, sich aber bald damit abfinden und Ihnen erlauben, das neue Passwort einzugeben.
</para>

<para> 
Wenn Sie dies nicht tun, kann es sein, dass Windows for Workgroups sich an das alte Passwort erinnert 
und es verwendet, auch wenn Sie ein neues angegeben haben.
</para>

<para> 
Oft wird Windows for Workgroups ein Passwort gänzlich ignorieren, das Sie in einer Dialog-Box
angeben.
</para>

</sect2>

<sect2>
<title>Den Umgang mit Passwörtern in Windows for Workgroups konfigurieren</title>

<para>
Auf der letzten Diskette (Diskette 8) des WFW-3.11-Diskettensatzes gibt es ein Programm namens <filename>admincfg.exe</filename>. Um es zu installieren,
geben Sie <userinput>EXPAND A:\ADMINCFG.EX_ C:\WINDOWS\ADMINCFG.EXE</userinput> ein.
Fügen Sie dann über das Menü <guimenu>New</guimenu> im <application>Program Manager</application> ein Icon hinzu.
Dieses Programm erlaubt Ihnen die Kontrolle darüber, wie WFW mit Passwörtern umgeht, z.B. beim
Ausschalten von Passwort-Caching und so weiter.</para>

</sect2>

<sect2>
<title>Groß-/Kleinschreibung in Passwörtern</title>

<para>Windows for Workgroups setzt das ganze Passwort in Großbuchstaben, bevor es es an den Server sendet.
UNIX-Passwörter können jedoch zwischen Groß-/Kleinschreibung unterscheiden.
Prüfen Sie die Informationen zum Parameter <smbconfoption><name>password level</name></smbconfoption>
in der &smb.conf;, um angeben zu können, ob Samba bei der Prüfung des Passworts die
Großschreibung versuchen soll.</para>

</sect2>

<sect2>
<title>TCP/IP als Standard-Protokoll verwenden</title>

<para>Um Reports über Drucker-Queues zu unterstützen, werden Sie unter Umständen
unter WfW TCP/IP als Standard-Protokoll verwenden müssen.
Aus irgendeinem Grund kann das Belassen von NetBEUI als Standardeinstellung dazu führen, dass die Queue-Reports auf manchen Systemen nicht funktionieren. Dies ist möglicherweise ein Bug in WfW.</para>

</sect2>

<sect2>
<title>Geschwindigkeitssteigerung</title>

<para>Einige Anwender haben festgestellt, dass sie eine große Beschleunigung erzielen, wenn sie den Parameter
<parameter>DefaultRcvWindow</parameter> im Abschnitt <smbconfsection>[MSTCP]</smbconfsection> der
Datei <filename>SYSTEM.INI</filename> unter WfW auf den Wert 3072 setzen.
</para>

<para>
Meine eigene Erfahrung mit <parameter>DefaultRcvWindow</parameter> ist, dass ich eine viel bessere
Performance mit großen Werten (16384 oder höher) erziele. Andere berichten, dass alles über
3072 wieder enorm bremst. Jemand hat sogar einen Geschwindigkeitsabfall auf 1/30 gemessen, als
er den Wert von 3072 auf 8192 erhöht hat.
</para>
</sect2>
</sect1>

<sect1>
<title>Windows 95/98</title>

<para>
Bei der Verwendung von Windows 95 OEM SR2 werden folgende Updates empfohlen,
wenn Samba eingesetzt wird. Bitte beachten Sie, dass die oben genannte Änderung Sie betreffen wird, sobald
diese Updates installiert worden sind.
</para>

<para> 
Es gibt mehr Updates als die hier erwähnten. Wir verweisen Sie auf die
Microsoft-Website für die verfügbaren Updates zu Ihrer spezifischen Version von Windows 95.
</para>

<simplelist>
<member>Kernel-Update: KRNLUPD.EXE</member>
<member>Ping-Fix: PINGUPD.EXE</member>
<member>RPC-Update: RPCRTUPD.EXE</member>
<member>TCP/IP-Update: VIPUPD.EXE</member>
<member>Redirector-Update: VRDRUPD.EXE</member>
</simplelist>

<para>
Auch bei Verwendung von <application>MS Outlook</application> ist es wünschenswert, den Fix von
<command>OLEUPD.EXE</command> zu installieren. Dieser Fix kann das Hängenbleiben beim Beenden
von Outlook verhindern, und Sie könnten eine deutliche Beschleunigung bemerken, wenn Sie auf die
Netzwerkumgebung zugreifen.
</para>

<sect2>
<title>Geschwindigkeitssteigerung</title>

<para>
Konfigurieren Sie die Windows 95-TCP/IP-Registrierungseinträge, um eine bessere Performance zu erreichen.
Ich verwende ein Programm namens <command>MTUSPEED.exe</command> aus dem Internet. Es gibt verschiedene
andere Utilities dieser Art, die frei verfügbar sind.
</para>

</sect2>

</sect1>

<sect1>
<title>Windows 2000 Service Pack 2</title>

<para> 
Es gibt einige lästige Dinge an Windows 2000 SP2. Eines davon tritt nur auf, wenn man einen 
Samba-Server verwendet, um Benutzer-Profile für Windows 2000 SP2-Clients in einer Windows-Domäne
bereitzustellen. Dies geht davon aus, dass Samba Domänen-Mitglied ist, aber das Problem wird
meist dann auftreten, wenn Samba es nicht ist.
</para>

<para> 
Um Profile erfolgreich für Windows 2000 SP2-Clients anzubieten (ohne PDC zu sein), muss Samba
den Parameter <smbconfoption><name>nt acl support</name><value>no</value></smbconfoption> für
die Freigabe gesetzt haben, die die Roaming-Profile beinhaltet. Wenn dies nicht erfolgt, wird sich
der Windows 2000 SP2-Client darüber beschweren, dass er nicht imstande ist, auf das Profil
zuzugreifen (Access Denied) und mehrfach Kopien davon auf der Platte anlegen (DOMAIN.user.001,
DOMAIN.user.002 und so weiter). Lesen Sie in der Manpage zu &smb.conf; mehr über diesen Parameter.
Beachten Sie auch, dass der Parameter <smbconfoption><name>nt acl support</name></smbconfoption>
in Releases vor Samba 2.2.2 formal ein globaler Parameter war.
</para>

<para> 
<link linkend="minimalprofile"/> zeigt eine minimale Profil-Freigabe.
</para>

<para><smbconfexample id="minimalprofile">
<title>Minimale Profil-Freigabe</title>
<smbconfsection>[profile]</smbconfsection>
<smbconfoption><name>path</name><value>/export/profile</value></smbconfoption>
<smbconfoption><name>create mask</name><value>0600</value></smbconfoption>
<smbconfoption><name>directory mask</name><value>0700</value></smbconfoption>
<smbconfoption><name>nt acl support</name><value>no</value></smbconfoption>
<smbconfoption><name>read only</name><value>no</value></smbconfoption>
</smbconfexample></para>

<para>
Der Grund für diesen Bug ist, dass der Windows 2000 SP2-Client den Security-Deskriptor für das
Profil, das die SID des Samba-Servers enthält, kopiert und nicht die Domänen-SID. Der Client
vergleicht die SID für SAMBA\user und stellt fest, dass sie von der SID für DOMAIN\user abweicht.
Daher kommt es zu der Meldung <errorname>access denied</errorname>.
</para>

<para>
Durch Deaktivieren des Parameters <smbconfoption><name>nt acl support</name></smbconfoption> sendet
Samba dem Windows 200x-Client eine Antwort auf den Aufruf <quote>QuerySecurityDescriptor trans2</quote>, die den
Client dazu veranlasst, eine Standard-ACL für das Profil zu setzen. Diese Standard-ACL enthält:
</para>

<para><emphasis>DOMAIN\user 	<quote>Full Control</quote></emphasis>></para>

<note><para>Dieser Bug tritt nicht auf, wenn man Winbind zum Anlegen von Benutzerkonten für
Domänen-Benutzer auf dem Samba-Host verwendet.</para></note>

</sect1>

<sect1>
<title>Windows NT 3.1</title>

<para>Wenn Sie Probleme mit der Kommunikation über Router mit Windows NT 3.1-Workstations haben,
lesen Sie <ulink url="http://support.microsoft.com/default.aspx?scid=kb;Q103765">den 
Microsoft Knowledge-Base-Artikel.</ulink>
</para>

</sect1>

</chapter>
